# 产品文档

> 版本：v2.1 · 2026-02-22

---

## 1. 产品定位

**小说知识库**是一套面向小说创作者和阅读爱好者的本地 AI 工具，核心价值是：

> 把一段小说文本变成可交互的沉浸式剧情体验

与市面上的 AI 写作工具不同，本工具不是帮用户"生成"内容，而是帮用户"体验"已有内容——AI 扮演叙事引擎，用户扮演主角，按故事本来的情节一幕幕经历。

---

## 2. 目标用户

| 用户类型 | 痛点 | 本工具的价值 |
|---------|------|------------|
| **小说创作者** | 写完了故事，想感受角色视角；或验证情节逻辑 | 把自己的作品变成可玩的剧情游戏 |
| **小说读者** | 喜欢某段故事，希望深入其中互动 | 沉浸式重温，体验不同选择 |
| **AI 互动爱好者** | 喜欢角色扮演，但希望 AI 有"记忆"和"档案" | 有结构化背景档案支撑的高质量角色扮演 |
| **Obsidian 用户** | 记录大量笔记，希望 AI 能基于个人数据对话 | 日记/小说解析 + MCP 接入 Claude Desktop |

---

## 3. 核心功能

### 3.1 小说解析（AI 结构化提取）

将小说文本交给 AI 分析，按模板提取四个维度的结构化信息：

| 维度 | 提取内容 | 应用场景 |
|------|---------|---------|
| **事件** | 关键情节、发生时间、情感分、是否核心 | 剧情推进的主线 |
| **属性** | 主角性格、状态、喜好 | 叙述主角时保持一致性 |
| **关系** | 配角姓名、关系类型、好感度、描述 | 配角开口前查阅确保语气一致 |
| **成就** | 重要里程碑、类别、重要性 | 回顾成长轨迹 |

**输入限制**：单次最多 20000 字（约 10-13 页 A4 纸）

**解析耗时**：通常 15~45 秒（取决于文本长度和 API 响应速度）

**输出格式**：带 YAML frontmatter 的 Markdown 文件，兼容 Obsidian

---

### 3.2 沉浸式聊天（AI 叙事引擎）

AI 承担三重角色：

**叙述者**
- 用第二人称（"你"）描写主角所见所感
- 每幕先交代环境与氛围，再呈现事件，结尾留悬念
- 情感基调参考事件档案中的情感分

**配角扮演者**
- 开口前必须查询关系档案，确认性格、好感度
- 配角语气与档案描述保持一致
- 绝不替主角说话，选择权交给用户

**剧情引导者**
- 按事件档案的时间顺序推进
- 每幕结束提供 2~3 个行动选项
- 主线结果遵循档案，用户行为影响细节

**特殊指令**

| 指令 | 效果 |
|------|------|
| `跳过` | 摘要当前场景，跳至下一事件 |
| `回顾` | 列出已经历的事件时间线 |
| `认识XXX` | 旁白介绍该角色档案 |
| `重来` | 重新叙述当前场景，换一种开场 |

---

### 3.3 Web UI（主要入口）

三标签页设计，面向非技术用户：

**⚙️ 配置**
- AI 提供商、模型、API Key 设置
- 数据存储位置说明（无需手动配置路径）

**📚 上传解析**
- 填写小说名称、主人公名字、期望情节数
- 粘贴文本，字数计数器实时提示（17000 字变黄，20000 字变红）
- 解析日志实时滚动显示进度
- 完成后一键「前往聊天」

**💬 开始聊天**
- 下拉选择已解析的小说
- 沉浸式气泡对话界面
- 工具调用可视化（🔍查档标签）
- Ctrl+Enter 发送

---

### 3.4 CLI 工具（高级用户）

| 工具 | 适用场景 |
|------|---------|
| `main_novel.py` | 批量处理多篇小说，脚本自动化 |
| `main.py` | 个人日记按日期拆分 |
| `chat_client_novel.py` | 终端沉浸体验，低延迟 |
| `chat_client.py` | 日记知识库问答 |

---

### 3.5 REST API（集成用途）

`api_server.py` 暴露 HTTP 端点，适合：
- 在其他工具中查询个人知识库
- 构建自定义前端
- 自动化脚本读取档案数据

端点根据模板自动生成，修改模板重启即更新。

---

### 3.6 MCP Server（Claude Desktop 集成）

`mcp_server.py` 让 Claude Desktop 能直接查询本地知识库：
- 6 个工具：概览、搜索、人物、事件、属性、成就
- 配置一次，对话时 AI 按需自动调用
- 适合日记知识库场景（"我最近有哪些开心的事？"）

---

## 4. 产品边界与限制

| 限制项 | 当前值 | 说明 |
|-------|-------|------|
| 单次解析字数上限 | 20000 字 | 受 AI 上下文窗口限制 |
| 聊天会话持久化 | 无（内存存储） | 重启服务后会话清空 |
| 并发会话 | 支持（按 session_id 隔离） | Web UI 默认单会话 |
| 离线使用 | 不支持 | 解析和聊天均需 AI API |
| 图片/音频 | 不支持 | 仅处理文本 |
| 超长小说 | 需分章上传 | 超过 20000 字需手动分段 |

---

## 5. 数据与隐私

- 所有档案文件存储在本地 `data/` 目录
- 小说文本仅在解析时发送给 AI API（DeepSeek/OpenAI），不经过任何中间服务器
- 对话消息在聊天时发送给 AI API，会话结束后不持久化
- `config.yaml` 中的 API Key 以明文存储在本地，请勿将该文件提交到公开代码仓库

---

## 6. 典型使用流程

### 流程一：作者体验自己的故事

```
写完小说章节
    ↓
打开 http://127.0.0.1:7860
    ↓
「上传解析」→ 粘贴内容 → 设置主人公名字 → 解析
    ↓
「开始聊天」→ 选择刚才的小说 → 开始对话
    ↓
以主角视角经历自己写的故事，发现情节漏洞或意外的感动
```

### 流程二：读者沉浸重温

```
复制喜欢的小说章节（注意版权）
    ↓
上传解析 → 聊天体验
    ↓
用「认识XXX」深入了解配角背景
用「跳过」快进到最想体验的场景
```

### 流程三：开发者集成

```
python api_server.py --port 8000
    ↓
GET /ai-docs  获取 AI 友好接口文档
    ↓
将文档粘贴进任意 AI 的系统提示词
    ↓
该 AI 可调用 API 查询用户个人知识库
```

### 流程四：Claude Desktop 接入

```
配置 mcp_server.py 到 claude_desktop_config.json
    ↓
重启 Claude Desktop
    ↓
正常对话，Claude 在需要时自动调用工具查询本地数据
```

---

## 7. 版本历史

| 版本 | 日期 | 主要变更 |
|------|------|---------|
| v1.0 | 2026-02 | 日记解析器（main.py）+ MCP Server |
| v1.5 | 2026-02 | REST API（api_server.py）+ 日记聊天客户端 |
| v2.0 | 2026-02 | 小说模式（main_novel.py + chat_client_novel.py）|
| v2.1 | 2026-02 | Web UI（web_app.py）+ 相对路径存储 + 20000字上限 |

---

## 8. 部署建议

**个人本地使用（推荐）**

```bash
python web_app.py  # 默认绑定 127.0.0.1，仅本机访问
```

**局域网共享**

```bash
python web_app.py --host 0.0.0.0 --port 7860
# 注意：无认证机制，仅在可信网络环境下使用
```

**后台持续运行（macOS/Linux）**

```bash
nohup python web_app.py > web.log 2>&1 &
```
