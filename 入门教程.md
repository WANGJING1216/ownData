# 小说知识库 · 入门教程

> 面向零基础用户，10 分钟完成第一次沉浸体验

---

## 这个工具能做什么？

你写了一篇小说（或粘贴了一段故事），这个工具会：

1. **用 AI 自动分析**，把故事里的人物、事件、关系、成就整理成结构化档案
2. **启动一个聊天界面**，AI 扮演叙事引擎，让你以主角视角"亲身经历"这段故事

就像把小说变成了一个可以互动的游戏。

---

## 第一步：安装环境

> 你需要有 Python 3.10 或更高版本。不确定？打开终端输入 `python3 --version` 查看。

打开终端（Mac：Spotlight 搜索"终端"；Windows：Win+R 输入 cmd），进入工具目录，运行：

```bash
pip install -r requirements.txt
```

安装完成后没有红色报错即为成功。

---

## 第二步：获取 AI 的 API Key

工具需要调用 AI 来分析小说。推荐使用 **DeepSeek**，原因：
- 中文理解能力强
- 价格极低（约 ¥1 / 百万字，分析一篇小说只需几分钱）
- 注册简单，无需翻墙

**获取步骤：**

1. 打开浏览器，访问 [platform.deepseek.com](https://platform.deepseek.com)
2. 注册账号（手机号或邮箱均可）
3. 登录后点击左侧「API Keys」→「创建 API Key」
4. 复制生成的 Key（形如 `sk-xxxxxxxxxxxxxxxx`），妥善保存

> 如果你已有 OpenAI 的 Key，也可以直接用，在配置页选择 OpenAI 提供商即可。

---

## 第三步：启动工具

在终端中运行：

```bash
python web_app.py
```

看到以下输出说明启动成功：

```
🌐  http://127.0.0.1:7860
```

打开浏览器，访问 **http://127.0.0.1:7860**，你会看到三个标签页。

---

## 第四步：填写配置（⚙️ 配置）

点击页面顶部的「⚙️ 配置」标签：

| 字段 | 填写内容 |
|------|---------|
| 提供商 | 选 DeepSeek（推荐）或 OpenAI |
| 模型 | 保持默认 `deepseek-chat` 即可 |
| API Key | 粘贴第二步获取的 Key |

填写完成后点击 **「💾 保存配置」**，看到绿色「✓ 已保存」即可。

---

## 第五步：上传并解析小说（📚 上传解析）

点击「📚 上传解析」标签：

**填写信息：**

| 字段 | 说明 | 示例 |
|------|------|------|
| 小说名称 | 随意命名，不含后缀 | `动物园`、`第一章` |
| 主人公名字 | 故事中第一人称的名字 | `我`、`林晓` |
| 情节数量 | AI 提取多少个关键事件 | `8`（建议每千字 3~5 个）|
| 小说内容 | 粘贴原文，上限 20000 字 | — |

**操作：**

1. 粘贴你的小说内容到文本框
2. 右上角字数计数器变**黄**时（17000+）说明接近上限，变**红**则需要删减
3. 点击 **「⚡ 上传并解析」**
4. 下方日志框会实时显示 AI 的工作进度（通常需要 10~30 秒）
5. 看到绿色 **「✓ 解析完成」** 后，点击旁边的 **「▶ 前往聊天」**

> **字数超限怎么办？** 截取最精彩的一段（如第一章），或把长篇拆成多篇分别上传。

---

## 第六步：开始沉浸聊天（💬 开始聊天）

点击「💬 开始聊天」标签（或点「前往聊天」自动跳转）：

1. 下拉框中选择刚才解析的小说名称
2. 点击 **「▶ 开始对话」**
3. AI 会自动读取档案，写出沉浸式开场白（可能需要 20~40 秒，因为 AI 要查几次档案）

**之后你可以：**

- 直接打字描述你的行动，如「我走进那扇门」
- 使用特殊指令快速操作：

| 输入 | 效果 |
|------|------|
| `跳过` | 跳到下一个情节 |
| `回顾` | 列出已经历的事件时间线 |
| `认识林晓` | AI 介绍该角色的档案 |
| `重来` | 重新叙述当前场景 |

---

## 常见问题

**Q：解析失败，日志显示"API 调用失败"？**

检查 API Key 是否正确，账户是否有余额（DeepSeek 新用户有免费额度）。

**Q：解析完成了，但聊天时 AI 说"没有找到任何档案"？**

说明解析产生了 0 条记录。可能原因：小说内容太短（< 300 字），或小说以第三人称写成但主人公填了"我"。尝试重新解析，调整主人公名字与小说中的称呼一致。

**Q：开场白很慢，等了好久？**

正常现象。AI 开场前需要查询 2 次档案（概览 + 事件列表），之后的每轮对话会快很多。

**Q：能上传多本小说吗？**

可以，每本小说独立存储在 `data/小说名/` 目录下，互不干扰。聊天时从下拉框切换即可。

**Q：关掉浏览器再打开，之前的对话还在吗？**

对话历史保存在内存中，关闭服务（Ctrl+C）后会清空。重新打开后点「▶ 开始对话」即可重新开始。

**Q：我想体验一下，但还没有自己的小说？**

工具自带了一篇示例小说（`示例小说`），如果已经解析过，可以直接在聊天页选择它体验。

---

## 目录结构说明

工具运行后会在自身目录创建 `data/` 文件夹，结构如下：

```
data/
└── 你的小说名/
    ├── src/
    │   └── 你的小说名.md   ← 原始文本（每次上传会覆盖）
    ├── 事件/               ← AI 解析的事件档案
    ├── 属性/               ← 主角属性档案
    ├── 关系/               ← 人物关系档案
    └── 成就/               ← 成就档案
```

所有数据都在本地，不会上传到任何服务器（除了调用 AI API 时发送小说文本）。

---

## 停止工具

在终端中按 **Ctrl + C** 即可停止服务器。
