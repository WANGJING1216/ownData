# 技术文档

> 版本：v2.1 · 面向开发者

---

## 1. 架构总览

```
┌─────────────────────────────────────────────────────────┐
│                      用户接入层                          │
│  浏览器 Web UI    终端 CLI    Claude Desktop    HTTP API │
│  (web_app.py)  (chat_client) (mcp_server.py) (api_server)│
└────────────┬────────────┬──────────────┬────────────────┘
             │            │              │
             ▼            ▼              ▼
┌─────────────────────────────────────────────────────────┐
│                      核心层                             │
│   解析器                    查询执行器                  │
│  main_novel.py             VaultExecutor               │
│  main.py                   (chat_client_novel.py)       │
└────────────┬────────────────────────┬───────────────────┘
             │                        │
             ▼                        ▼
┌─────────────────────────────────────────────────────────┐
│                    数据层                               │
│         data/{小说名}/                                  │
│         ├── src/        ← 原始文本                      │
│         ├── 事件/       ← YAML frontmatter + MD 档案    │
│         ├── 属性/                                       │
│         ├── 关系/                                       │
│         └── 成就/                                       │
└─────────────────────────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────────────┐
│                    配置层                               │
│   config.yaml        templates/game_life.json           │
│   (API Key, 路径)    (维度定义、字段类型)               │
└─────────────────────────────────────────────────────────┘
```

### 数据流（解析）

```
用户粘贴小说文本
      │
      ▼
web_app.py /api/novel/parse
  → 保存到 data/{名}/src/{名}.md
  → 调用 main_novel.py --file --vault --protagonist --plot-count
      │
      ▼
main_novel.py
  → load_config() 读取 API Key / 模板路径
  → build_prompt() 构造提示词（含模板 JSON）
  → call_api() → DeepSeek/OpenAI
  → extract_json() 解析返回的 JSON
  → generate_entry_file() 写入 data/{名}/{维度}/*.md
      │
      ▼
data/{小说名}/事件/小说名-事件标题.md   ← YAML frontmatter + 描述正文
```

### 数据流（沉浸聊天）

```
用户选择小说名 → 点击「开始对话」
      │
      ▼
web_app.py /api/chat/reset
  → _init_session(novel_name)
      → load_template()  读取 game_life.json
      → build_tools()    生成 OpenAI function definitions
      → VaultExecutor()  初始化档案读取器
      → build_system_prompt()  构造系统提示词

用户发送消息 → /api/chat/message (SSE)
      │
      ▼
while True:
  client.chat.completions.create(messages, tools)
  if tool_calls:
      VaultExecutor.run(tool_name, args)  ← 读取本地 .md 档案
      append tool result → 继续循环
  else:
      yield SSE content event → 浏览器渲染气泡
      break
```

---

## 2. 模块说明

### `web_app.py` — Web UI 后端

| 职责 | 说明 |
|------|------|
| 配置读写 | `_read_cfg()` / `_write_cfg()` 操作 `config.yaml` |
| 小说解析 | 保存文本，异步启动 `main_novel.py` 子进程，SSE 流式输出日志 |
| 沉浸聊天 | 维护 `_sessions` 内存会话，多轮工具调用循环，SSE 流式回复 |
| 静态服务 | `GET /` 返回 `web/index.html` |

**关键常量**

```python
BASE_DIR = Path(__file__).parent       # 程序根目录
DATA_DIR = BASE_DIR / "data"           # 所有小说数据
CONFIG_PATH = BASE_DIR / "config.yaml"
```

**会话结构**

```python
_sessions[session_id] = {
    "novel_name": str,          # 当前小说名
    "messages":   list[dict],   # OpenAI 格式消息历史
    "tools":      list[dict],   # function calling 工具定义
    "executor":   VaultExecutor # 档案查询执行器
}
```

---

### `main_novel.py` — 小说解析器

**核心函数链**

```
main()
  └─ load_config()          读取并验证 config.yaml
  └─ [--vault 覆盖]         override obsidian_vault_path
  └─ load_template()        加载 templates/game_life.json
  └─ process_diary()
       └─ build_prompt()    构造包含模板的提示词
       └─ call_api()        HTTP POST 到 AI 接口
       └─ extract_json()    解析 AI 返回的 JSON
       └─ generate_entry_file()  写 YAML frontmatter .md
```

**CLI 参数**

| 参数 | 默认 | 说明 |
|------|------|------|
| `--file PATH` | — | 指定单个文件（绝对路径） |
| `--vault PATH` | config 中的值 | 覆盖 vault 根目录，自动 mkdir |
| `--protagonist` / `-p` | `我` | 主人公称呼 |
| `--plot-count` / `-n` | `0`（不限） | 期望提取的事件数 |
| `--force` | False | 忽略处理记录，强制重新解析 |
| `--dry-run` | False | 仅生成提示词，不调用 API |
| `--config` | `config.yaml` | 指定配置文件路径 |

**`_validate_config` 检查项**

- `obsidian_vault_path`、`template_path`、`api` 字段存在
- `api.api_key` 非空且不以 `sk-xxx` 开头
- **不检查** vault 路径是否存在（`--vault` 模式下由 main() 创建）

---

### `api_server.py` — REST API 服务

动态从模板生成 FastAPI 路由。

**固定端点**

| 端点 | 方法 | 说明 |
|------|------|------|
| `/summary` | GET | 知识库统计概览（各维度条目数） |
| `/search` | GET | 全文搜索，参数：`q`, `dimension`, `limit` |
| `/person/{name}` | GET | 人物档案 + 关联事件 |
| `/ai-docs` | GET | AI 友好纯文本接口文档 |
| `/docs` | GET | Swagger UI |

**动态端点（按模板维度生成）**

端点名 = `/{维度名}`，查询参数由字段类型自动映射：

| 字段类型 | 生成参数 | 示例 |
|---------|---------|------|
| `number` | `min_{字段}` + `max_{字段}` | `min_情感分=4` |
| `date` | `{字段}_from` + `{字段}_to` | `日期_from=第一年` |
| `select` / `multiselect` | `{字段}=枚举值` | `类型=家庭` |
| `boolean` | `{字段}=true/false` | `是否核心=true` |
| `text` | 通过 `q` 全文搜索 | — |
| `relation` | 跳过（用 `/person/{name}` 代替） | — |

实现原理：使用 `inspect.Parameter` + `handler.__signature__` 动态构造函数签名，FastAPI 自动生成 OpenAPI 文档。

**核心数据函数**

```python
load_entries(vault_path, folder) -> list[dict]
# 读取指定文件夹所有 .md，解析 YAML frontmatter，去除 [[wikilinks]]

apply_filters(entries, fields, args) -> list[dict]
# 按查询参数过滤条目，支持数值范围、日期范围、枚举、布尔、全文
```

---

### `chat_client_novel.py` — 沉浸聊天核心

**`build_tools(template)`**

将模板维度转换为 OpenAI function calling 格式。固定工具：

- `vault_summary` — 获取故事全貌统计
- `search_entries` — 关键词搜索
- `get_person` — 单个角色完整档案

动态工具（每个维度一个）：

- `query_events`（事件维度）
- `query_attributes`（属性维度）
- `query_relations`（关系维度）
- `query_achievements`（成就维度）

维度名 → 工具名通过 `_DIM_TO_EN` 字典映射（避免中文 function name 被 API 拒绝）。

**`VaultExecutor.run(tool_name, args)`**

路由表：

```
"vault_summary"  → _summary()   遍历所有维度统计条目数
"search_entries" → _search()    全文搜索，可按维度和日期过滤
"get_person"     → _person()    查关系维度 + 关联事件维度
tool in name_map → _query_dim() 调用 apply_filters 过滤条目
```

**`build_system_prompt(template)`**

生成约 600 字的系统提示词，定义三重身份（叙述者/配角扮演者/剧情引导者）、工具使用节奏、特殊指令响应规则。

---

### `mcp_server.py` — MCP Server

使用 FastMCP 暴露 6 个工具，支持 stdio（Claude Desktop）和 SSE HTTP（调试）两种模式。

| 工具 | 说明 |
|------|------|
| `vault_summary` | 知识库整体统计 |
| `search_entries` | 全文搜索 |
| `get_person` | 人物档案 + 关联事件 |
| `get_events` | 事件过滤（日期/类型/情感分） |
| `get_attributes` | 属性查询 |
| `get_achievements` | 成就查询 |

---

## 3. 配置文件参考（config.yaml）

```yaml
# Vault 路径（CLI 工具使用；Web UI 使用 data/ 相对路径，此字段自动维护）
obsidian_vault_path: "/path/to/vault"

# 小说/日记源文件夹（相对于 vault）
diary_folder: "src"

# 解析模板路径（相对于脚本目录或 vault）
template_path: "templates/game_life.json"

# 维度 → 输出文件夹名映射
output_folders:
  属性: "属性"
  事件: "事件"
  关系: "关系"
  成就: "成就"

# 处理记录文件（避免重复解析）
processed_log: "processed_diaries.txt"

# AI API 配置
api:
  provider: "deepseek"          # deepseek | openai | 自定义
  model: "deepseek-chat"
  api_key: "sk-xxxxxxxxxxxxxxxx"
  base_url: ""                  # 留空则使用 provider 默认 URL
                                # Ollama 示例: http://localhost:11434/v1
```

**provider 对应默认 base_url**

| provider | base_url |
|---------|---------|
| `deepseek` | `https://api.deepseek.com` |
| `openai` | `https://api.openai.com/v1` |
| 自定义 | 填写 `base_url` 字段 |

---

## 4. 模板系统

模板文件 `templates/game_life.json` 驱动：

- AI 解析时的提示词结构
- REST API 的动态路由和查询参数
- Function calling 工具定义
- 档案文件的 YAML frontmatter 字段

### 模板结构

```json
{
  "name": "模板名称",
  "dimensions": [
    {
      "name": "维度名",
      "fields": [
        {
          "name": "字段名",
          "type": "text | number | date | select | multiselect | boolean | relation",
          "required": true,
          "options": ["选项1", "选项2"],   // select/multiselect 时有效
          "free": true,                     // multiselect 是否允许自定义值
          "min": 1, "max": 5,               // number 时有效
          "target": "关系"                  // relation 指向的维度名
        }
      ]
    }
  ]
}
```

### 字段类型说明

| 类型 | 存储格式 | AI 填写规则 |
|------|---------|------------|
| `text` | 字符串 | 自由文本 |
| `number` | 数字 | 在 min/max 范围内 |
| `date` | 字符串 | 小说模式：故事内时间描述；日记模式：YYYY-MM-DD |
| `select` | 字符串 | 从 options 选一个 |
| `multiselect` | 字符串数组 | 从 options 多选；free=true 时可追加 |
| `boolean` | true/false | — |
| `relation` | 字符串/数组 | 存储为 `[[姓名]]` Obsidian 双链格式，查询时自动去除 |

---

## 5. 数据格式

### 档案文件格式（YAML frontmatter + Markdown）

```markdown
---
标题: 星力觉醒
日期: 故事第一天傍晚
情感分: 4
类型:
  - 学习
  - 个人成长
关键词:
  - 星力
  - 觉醒
  - 星海森林
是否核心: true
关联人物:
  - [[李玄]]
---

我在星海森林边缘意外触碰了一块蓝色星石，掌心透出淡蓝色光晕，星力由此觉醒。
```

**文件命名规则**：`{diary_date}-{标题/姓名/属性名}.md`，diary_date 为小说名（stem）或日记日期。

**唯一性保证**：同名文件自动追加 `-1`、`-2` 后缀。

---

## 6. Web API 参考（web_app.py）

### `GET /api/config`

返回当前配置（Key 脱敏）。

```json
{
  "provider": "deepseek",
  "model": "deepseek-chat",
  "api_key_masked": "sk-53040...b2c8",
  "has_key": true
}
```

### `POST /api/config`

Body（JSON）：

```json
{
  "provider": "deepseek",
  "model": "deepseek-chat",
  "api_key": "sk-..."     // 留空则保留已保存的 Key
}
```

### `GET /api/novel/list`

返回 `data/` 下所有子目录名的有序数组。

```json
["示例小说", "动物园", "第一章"]
```

### `POST /api/novel/parse`

Form 参数：

| 参数 | 类型 | 说明 |
|------|------|------|
| `novel_name` | string | 小说名（用作目录名和文件名） |
| `protagonist` | string | 主人公称呼，默认 `我` |
| `plot_count` | int | 期望事件数，默认 `10` |
| `content` | string | 小说文本，上限 20000 字 |

返回：SSE 事件流

```
data: {"type": "log",   "text": "✎ 已保存..."}
data: {"type": "log",   "text": "2026-02-22 [INFO] 处理日记: ..."}
data: {"type": "done",  "novel": "小说名", "text": "✓ 解析完成"}
data: {"type": "error", "text": "✗ 解析失败（退出码 1）"}
```

### `POST /api/chat/reset`

Body（JSON）：

```json
{ "session_id": "default", "novel_name": "示例小说" }
```

初始化会话（加载模板、构建工具、创建 VaultExecutor）。

### `POST /api/chat/message`

Form 参数：

| 参数 | 类型 | 说明 |
|------|------|------|
| `message` | string | 用户消息 |
| `novel_name` | string | 当前小说名 |
| `session_id` | string | 会话 ID，默认 `default` |

返回：SSE 事件流

```
data: {"type": "tool",    "name": "vault_summary", "args": {}}
data: {"type": "tool",    "name": "query_events",  "args": {"limit": 20}}
data: {"type": "content", "text": "AI 的完整回复文本..."}
data: {"type": "done"}
data: {"type": "error",   "text": "错误信息"}
```

---

## 7. 扩展指南

### 添加新维度

1. 编辑 `templates/game_life.json`，在 `dimensions` 数组中追加新维度定义
2. 在 `config.yaml` 的 `output_folders` 中添加对应映射（可选，默认用维度名）
3. 在 `chat_client_novel.py` 的 `_DIM_TO_EN` 中添加中文→英文映射
4. 在 `_DIM_DESC` 中添加该维度的工具描述（供 AI 理解何时调用）
5. 重启所有服务生效

### 更换 AI 提供商

任何 OpenAI 兼容接口均可使用：

```yaml
api:
  provider: "custom"
  base_url: "http://localhost:11434/v1"  # Ollama
  model: "qwen2.5:7b"
  api_key: "ollama"                      # Ollama 不验证 Key，随意填写
```

### 多用户隔离

`_sessions` 字典以 `session_id` 为键。前端传入不同的 `session_id` 即可实现多会话并行（当前 Web UI 固定使用 `"default"`）。

---

## 8. 依赖清单

```
requests>=2.28.0     # main.py / main_novel.py — API 调用
pyyaml>=6.0          # 所有模块 — config.yaml 读写
mcp>=1.2.0           # mcp_server.py — MCP 协议
fastapi>=0.100.0     # api_server.py / web_app.py — HTTP 服务
uvicorn>=0.20.0      # api_server.py / web_app.py — ASGI 服务器
openai>=1.0.0        # chat_client*.py / web_app.py — OpenAI SDK
```
