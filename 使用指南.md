# 个人知识库工具集 使用指南

> 版本：v2.0 · 2026-02-21

---

## 整体架构

```
Obsidian Vault
├── 日记/ ──── main.py ──────────────────────────────┐
└── 小说/ ──── main_novel.py ──────────────────────┐  │
                                                   ↓  ↓
                                             结构化 Markdown
                                          (属性/事件/关系/成就...)
                                                   │
                    ┌──────────────────────────────┤
                    ↓                              ↓
             mcp_server.py                  api_server.py
           Claude Desktop                  REST HTTP API
           (MCP 协议)                      /docs  /ai-docs
                    │                              │
                    └──────────┬───────────────────┘
                               ↓
                    chat_client.py        ← 日记问答对话
                    chat_client_novel.py  ← 小说沉浸扮演
                    (DeepSeek / OpenAI / Ollama)
```

---

## 文件一览

| 文件 | 用途 |
|------|------|
| `main.py` | 日记 → 结构化条目（AI 拆分） |
| `main_novel.py` | 小说 → 结构化条目（故事时间线模式） |
| `mcp_server.py` | Claude Desktop MCP 服务，6 个查询工具 |
| `api_server.py` | REST API 服务，根据模板动态生成端点 |
| `chat_client.py` | 终端日记问答客户端 |
| `chat_client_novel.py` | 终端小说沉浸扮演客户端 |
| `config.yaml` | 统一配置（vault 路径、API Key、模板路径） |
| `templates/game_life.json` | 拆分模板（定义维度和字段） |

---

## 快速配置

`config.yaml` 关键字段：

```yaml
obsidian_vault_path: "/Users/wangjing/Documents/Obsidian Vault"
diary_folder: "日记"          # 日记源文件夹（main.py 用）
# diary_folder: "小说"        # 小说源文件夹（main_novel.py 用）

output_folders:
  属性: "属性"
  事件: "事件"
  关系: "关系"
  成就: "成就"

template_path: "templates/game_life.json"

api:
  provider: "deepseek"
  api_key: "sk-..."
  model: "deepseek-chat"
```

---

## 一、内容录入

### `main.py` — 日记拆分

```bash
python main.py               # 处理所有新日记
python main.py --dry-run     # 预览，不调用 API
python main.py --force       # 强制重处理所有日记
python main.py --file 日记/2026-02-21.md
```

文件命名：`YYYY-MM-DD.md`，日期自动提取作为条目前缀。

---

### `main_novel.py` — 小说拆分

```bash
python main_novel.py
python main_novel.py --file 小说/动物园.md
```

与 `main.py` 的差异：

| 项目 | main.py | main_novel.py |
|------|---------|---------------|
| 文件命名前缀 | YYYY-MM-DD 或今天日期 | 文件 stem 原样（如 `动物园`） |
| date 字段要求 | YYYY-MM-DD 格式 | 故事内时间描述（如"第三年春天"），无法判断填 null |
| AI 角色设定 | 日记分析助手 | 以"我"为主人公的专业小说家 |

小说文件命名建议：
- 章节型：`第一章.md`、`ch01.md`
- 场景型：`动物园.md`、`战场初遇.md`
- 时间轴型：`战争第7日.md`、`三年后.md`

---

## 二、Claude Desktop 接入（MCP）

### `mcp_server.py`

**配置文件**：`~/Library/Application Support/Claude/claude_desktop_config.json`

```json
{
  "mcpServers": {
    "diary": {
      "command": "/Users/wangjing/miniconda3/bin/python3",
      "args": ["/Users/wangjing/tools/claude/ownData/diary_splitter/mcp_server.py"]
    }
  }
}
```

修改后重启 Claude Desktop，对话框右下角出现工具图标即成功。

**6 个工具**：

| 工具 | 功能 | 典型问法 |
|------|------|---------|
| `vault_summary` | 知识库概览 | "你对我了解多少？" |
| `search_entries` | 全文搜索 | "找找和旅行有关的记录" |
| `get_person` | 人物档案+关联事件 | "你认识我爸爸吗？" |
| `get_events` | 事件过滤 | "我最近有哪些开心的事？" |
| `get_attributes` | 个人属性 | "我有哪些喜好？" |
| `get_achievements` | 成就记录 | "今年有哪些值得骄傲的事？" |

**推荐系统提示词**（粘贴到 Claude Desktop 项目设置）：

```
你拥有访问用户个人日记知识库的能力。
当用户提及自身经历、感受或问"我喜欢什么/我认识谁"时，先调用工具查询再回答。
查询结果为空时如实告知，不要编造信息。
```

---

## 三、REST API 服务

### `api_server.py`

```bash
python api_server.py                    # 127.0.0.1:8000
python api_server.py --port 3000
python api_server.py --host 0.0.0.0    # 外部可访问
```

**自动生成端点**（根据 `config.yaml` 模板动态创建）：

```
GET /summary          知识库统计概览
GET /search           全文搜索（跨维度）
GET /person/{name}    人物档案 + 关联事件
GET /ai-docs          AI 友好接口文档（纯文本，可粘贴进系统提示词）
GET /docs             Swagger UI 交互调试
GET /属性             模板驱动，参数: 属性类型
GET /事件             参数: 日期范围、min_情感分、类型、是否核心
GET /关系             参数: 关系类型、好感度范围
GET /成就             参数: 类别、日期范围、重要性范围
```

模板字段类型 → 自动生成的查询参数规则：

| 字段类型 | 生成参数 |
|---------|---------|
| `number` | `min_{字段}` + `max_{字段}` |
| `date` | `{字段}_from` + `{字段}_to` |
| `select/multiselect` | `{字段}=枚举值` |
| `boolean` | `{字段}=true/false` |
| `relation` | 跳过（用 `/person/{name}` 代替） |
| `text` | 通过 `q` 全文搜索 |

**换模板重启即生效，无需改代码。**

---

## 四、终端 AI 对话

### `chat_client.py` — 日记问答

支持 DeepSeek、OpenAI、Ollama 等所有 OpenAI 兼容接口。工具定义从模板自动生成，无需启动 `api_server.py`。

```bash
python chat_client.py                           # 使用 config.yaml 配置
python chat_client.py --model deepseek-chat
python chat_client.py --provider openai --model gpt-4o
python chat_client.py --base-url http://localhost:11434/v1 --model qwen2.5
```

---

### `chat_client_novel.py` — 小说沉浸扮演

AI 扮演叙事引擎，引导用户以主人公视角过完整剧情。

```bash
python chat_client_novel.py
python chat_client_novel.py --model deepseek-chat
```

**AI 的三重身份**：
- **叙述者**：第二人称描写主角所见所感，每幕留悬念
- **配角扮演者**：开口前先查档案，保证性格语气一致
- **剧情引导者**：按故事时间线推进，关键节点给出 2~3 个选项

**用户特殊指令**：

| 输入 | 效果 |
|------|------|
| `跳过` | 摘要当前场景，跳至下一事件 |
| `回顾` | 汇总已经历的事件时间线 |
| `认识XXX` | 旁白介绍该角色档案 |
| `重来` | 换一种方式重叙当前场景 |
| `quit` | 退出故事 |

**自动开场流程**：
1. AI 调用 `vault_summary` 掌握故事规模
2. AI 调用 `query_events` 获取全部事件列表
3. 以第一个事件为起点，写出沉浸式开场白

---

## 五、典型工作流

### 日记场景

```
每日写日记（Obsidian）
    ↓
python main.py           # AI 拆分 → 写入各维度文件夹
    ↓
打开 Claude Desktop      # MCP Server 自动启动，读取最新数据
    ↓
正常对话，AI 按需查询个人信息
```

### 小说场景

```
写小说章节（Obsidian 小说/XXX.md）
    ↓
python main_novel.py     # AI 拆分 → 写入各维度文件夹
    ↓
python chat_client_novel.py
    ↓
以主角视角沉浸体验完整剧情
```

---

## 六、依赖安装

```bash
pip install -r requirements.txt
```

```
requests>=2.28.0    # main.py / main_novel.py
pyyaml>=6.0         # 所有模块
mcp>=1.2.0          # mcp_server.py
fastapi>=0.100.0    # api_server.py
uvicorn>=0.20.0     # api_server.py
openai>=1.0.0       # chat_client.py / chat_client_novel.py
```

---

## 七、常见问题

**Q: Claude Desktop 看不到工具图标？**
```bash
python mcp_server.py --help   # 验证能否正常启动
```
若报错通常是 `mcp` 包未安装：`pip install mcp>=1.2.0`

**Q: chat_client 报"Vault 路径加载失败"？**
检查 `config.yaml` 中 `obsidian_vault_path` 是否为正确的绝对路径。

**Q: api_server 的端点参数和模板对不上？**
修改 `config.yaml` 中的模板路径后重启 `api_server.py`，端点和参数会自动更新。

**Q: 小说拆分出的事件日期显示为现实日期？**
使用 `main_novel.py` 而非 `main.py`，前者已修正为故事内时间描述。

**Q: chat_client_novel 开场很慢？**
正常现象，AI 首轮需查询 `vault_summary` 和 `query_events` 两次工具，之后响应正常。
